<h1>All the hoods</h1>

<h2>Which hood is the greenest?</h2>

<% ranked = @hoods.sort_by { |hood| hood.hood_score }.reverse %>
<ol>
  <% ranked.each do |hood| %>
  <li><%= link_to hood.name, hood %> - <%= hood.hood_score %></li>
  <% end %>
</ol>
